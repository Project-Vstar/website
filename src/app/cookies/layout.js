export const metadata = {
  title: 'Cookie Policy',
  description: 'Cookie usage information for VSTAR PROJECT.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function Layout({ children }) {
  return children;
}
