export const metadata = {
  title: 'Business Partner Area',
  description: 'Business partner area for VSTAR PROJECT.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function Layout({ children }) {
  return children;
}
