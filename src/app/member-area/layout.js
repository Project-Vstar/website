export const metadata = {
  title: 'Member Area',
  description: 'Member-only area for VSTAR PROJECT.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function Layout({ children }) {
  return children;
}
