export const metadata = {
  title: 'Impressum',
  description: 'Legal notice and company information for VSTAR PROJECT.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function Layout({ children }) {
  return children;
}
