export const metadata = {
  title: 'Press',
  description: 'Press information and media resources for VSTAR PROJECT.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function Layout({ children }) {
  return children;
}
